# build output (dist-de = haushoppe.de, dist-art = haushoppe.art)
dist/
dist-de/
dist-art/
.astro/
# dependencies
node_modules/
# logs
*.log
npm-debug.log*
# environment
.env
.env.*
!.env.example
# system / editor
.DS_Store
.vscode/
.idea/

# HINWEIS: public/artworks/ (geteilte Werk-webp — DE und EN nutzen dieselbe Datei) wird
# BEWUSST versioniert. Sonst fehlen die Galerie-Bilder im CI-Deploy (Cloudflare liefert
# dann den SPA-Fallback statt der Bilder). Nicht wieder ignorieren.

# build output (dist-de = haushoppe.de, dist-art = haushoppe.art)
dist/
dist-de/
dist-art/
.astro/
# dependencies
node_modules/
# logs
*.log
npm-debug.log*
# environment
.env
.env.*
!.env.example
# Cloudflare Pages Functions: lokale Secrets für `wrangler pages dev` (Vorlage: .dev.vars.example)
.dev.vars
# system / editor
.DS_Store
.vscode/
.idea/
# playwright e2e artefakte
test-results/
playwright-report/
blob-report/
.playwright/

# HINWEIS: Werk-Bilder werden NICHT mehr fertig versioniert. Versioniert ist nur die Quelle
# (src/artwork-originals/ = Nachlass-Master); die Auslieferung (AVIF+WebP responsive) erzeugt
# Astro beim Build via <Picture> nach dist/_astro/. Kein public/artworks/ mehr.

# NDR/Nordtour-Aufnahme (fremdes Branding) NICHT ins Repo
src/assets/holzschnitte/

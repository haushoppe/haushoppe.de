# build output (dist-de = haushoppe.de, dist-art = haushoppe.art)
dist/
dist-de/
dist-art/
.astro/
# dependencies
node_modules/
# logs
*.log
npm-debug.log*
# environment
.env
.env.*
!.env.example
# system / editor
.DS_Store
.vscode/
.idea/

# HINWEIS: Werk-Bilder werden NICHT mehr fertig versioniert. Versioniert ist nur die Quelle
# (src/artwork-originals/ = Nachlass-Master); die Auslieferung (AVIF+WebP responsive) erzeugt
# Astro beim Build via <Picture> nach dist/_astro/. Kein public/artworks/ mehr.
